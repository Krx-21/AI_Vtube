[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pailin-discord"
version = "0.2.0"
description = "ไพลิน (Pailin) – Discord bot application (placeholder)"
readme = "../../README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

dependencies = [
    "pailin-core",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["pailin_discord*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
